<!DOCTYPE html>
<html>
<head>
  <title>Homepage</title>
</head>
<body>
<aside id="colorlib-breadcrumbs">
			<div class="container">
				<div class="row">
					<div class="col-md-12 breadcrumbs text-center">
						<h1>NYU Piazza</h1>
						<p>
              <span>
                <a href="{{URL}}/home" class="btn btn-info">Home</a>
              </span>
              <span>
                <a href="{{URL}}/api/users/logout" class="btn btn-dark">Log Out</a>
              </span>
          </p>
					</div>
				</div>
			</div>
</aside>
    <br>
<h3>
  You are enrolled in the following courses:<br>
  click on them and ask questions!
</h3>
<h4>
<table class="table table-hover">
  <thead>
		<tr>
			<th>Name</th>
			<th>Code</th>
		</tr>
  </thead>
  {{#if courses}}
  <tbody>
    {{#each courses as |c|}}
  <tr>
    <td><a href="{{URL}}/home/{{this.slug}}">{{this.name}}</a></td>
    <td>{{this.code}}</td>
  </tr>
    {{/each}}
  </tbody>
  {{/if}}
</table>
</h4>
  <a href="{{URL}}/home/add-course" class="btn btn-primary">Enroll in a new course</a>
</body>
</html>
